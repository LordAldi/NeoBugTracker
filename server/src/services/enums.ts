export enum Role {
  none = "none",
  submitter = "submitter",
  developer = "developer",
  projectManager = "projectManager",
  admin = "admin",
}
export enum Content {
  project = "project",
  ticket = "ticket",
  profile = "profile",
}
